# Configurações do Servidor
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Banco de Dados
DATABASE_URL=postgresql://gerente:admin@localhost:5432/dashboard_p2p

# Segurança e Sessão
JWT_SECRET=troque_isso_por_um_hash_seguro_em_producao
SESSION_DAYS=30
ALLOWED_ORIGINS=*

# Configurações de E-mail (SMTP)
SMTP_HOST=smtp.exemplo.com
SMTP_PORT=587
SMTP_USER=seu_usuario
SMTP_PASS=sua_senha
SMTP_SECURE=false
SMTP_FROM="Dashboard P2P" <noreply@example.com>