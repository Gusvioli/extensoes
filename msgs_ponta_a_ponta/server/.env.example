# ============================================================================
# Configuração do Servidor 5uv1
# ============================================================================
# Copie este arquivo para .env e customize conforme necessário
# Exemplo: cp .env.example .env

# 🌐 REDE E PORTA
# Porta em que o servidor escuta
PORT=8080

# 🔐 AUTENTICAÇÃO
# Ativar autenticação obrigatória (recomendado: true)
REQUIRE_AUTH=true

# Token para autenticar clientes
# Se não definido, um será gerado automaticamente
# Recomendado: 32+ caracteres aleatórios
AUTH_TOKEN=seu-token-secreto-muito-seguro-aqui-32-caracteres-minimo

# 👥 LIMITES
# Número máximo de clientes conectados simultaneamente
MAX_CLIENTS=10000

# ⏱️ RATE LIMITING
# Janela de tempo em milissegundos para rate limit
RATE_LIMIT_WINDOW=1000

# Número máximo de mensagens por janela de tempo
RATE_LIMIT_MAX=100

# 🛡️ SEGURANÇA
# Desabilitar compressão deflate (previne CRIME attack)
# Recomendado: true (desabilitado por segurança)
DISABLE_DEFLATE=true

# 💓 HEARTBEAT
# Intervalo de heartbeat em milissegundos
HEARTBEAT_INTERVAL=30000

# Timeout de heartbeat em milissegundos
HEARTBEAT_TIMEOUT=5000

# 📊 MONITORAMENTO
# Ativar exibição de métricas (pode expor informações)
# Recomendado: false (desabilitado)
ENABLE_METRICS=false

# ============================================================================
# EXEMPLOS DE CONFIGURAÇÃO
# ============================================================================

# DESENVOLVIMENTO (com token simples)
# PORT=8080
# REQUIRE_AUTH=true
# AUTH_TOKEN=dev-token-123456
# ENABLE_METRICS=true

# PRODUÇÃO (com segurança máxima)
# PORT=8080
# REQUIRE_AUTH=true
# AUTH_TOKEN=$(openssl rand -hex 32)
# DISABLE_DEFLATE=true
# ENABLE_METRICS=false
# MAX_CLIENTS=5000
# RATE_LIMIT_MAX=50

# TESTE LOCAL (sem autenticação)
# PORT=8080
# REQUIRE_AUTH=false
# ENABLE_METRICS=true

# ============================================================================
# GERANDO UM TOKEN SEGURO
# ============================================================================
# Linux/Mac:
#   openssl rand -hex 32
#
# Node.js:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# Python:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
